{
  "name": "workout-tracker",
  "version": "1.0.0",
  "description": "minium product will be a workout tracker without sharing features, This should be the main focus for this week.",
  "main": "index.js",
  "scripts": {
  "start": "node server/server.js",
  "develop": "concurrently \"cd backEnd && npm run watch\" \"cd frontEnd && npm run dev\"",
  "install": "cd frontEnd && npm i && cd ../frontEnd && npm i --production=false",
  "build": "cd frontEnd && npm run build",
  "render-build":"npm run install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.0.1",
    "express": "^4.21.0"
  }
}
